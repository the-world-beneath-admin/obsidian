# Alchemy Lab World Key Worker Report - 2026-05-12 - Cave Pet Loop

## Task

Run a bounded prototype pass for The Alchemy Lab World Key inside `C:\Users\yrred\Desktop\Unity\TWB-Alchemy`, focused only on the current cave/pet movement loop, cave feel, pet return/sleep timing, and obvious smoke-test blockers.

Scope: parent project The World Beneath; World Key The Alchemy Lab.

## Result

No source-code fix was needed. The current cave/pet loop matched the active direction: top-down cave maze, no player torch loop, autonomous pet cave gathering, one-at-a-time pet release, return to shed before sleep, and five-minute sleep cooldown after unload.

## Files touched

- No source files were left changed.
- `dist/` was regenerated by `npm run build`.
- This report was added under `memory/short-term/`.
- Temporary smoke-test files were created and removed during the pass.

## Checks run

- `npm run build` from `C:\Users\yrred\Desktop\Unity\TWB-Alchemy` passed. Vite still reports the known large Phaser chunk warning.
- Local Vite server was started at `http://127.0.0.1:5174/` for smoke testing.
- Browser smoke test controlled `localStorage` by removing `twb_alchemy_lab_state_v1` and `twb_world_key_shared_inventory_v1` before and after the test.
- Browser smoke test showed no `console.error` or `pageerror` entries.
- Cave smoke observed initial pet phases: `exploring`, `waiting`, `waiting`.
- Waiting pet release spacing was verified at `30000ms`.
- Forced energy exhaustion moved the active pet into `returning`, with no pet sleeping before return/unload.
- Forced return/unload moved the pet into `sleeping` with a `300000ms` cooldown.
- Screenshot review confirmed the cave rendered, pet vision was visible, the top-down maze was present, and the companion panel reflected the staggered release state.

## Cleanup performed

- Stopped the temporary Vite server on port `5174`.
- Removed temporary smoke spec `codex-alchemy-smoke.spec.ts`.
- Removed temporary screenshot `codex-alchemy-cave-smoke.png`.
- Removed temporary Vite logs `codex-vite-5174.out.log` and `codex-vite-5174.err.log`.
- Removed temporary Playwright `test-results/`.
- Cleared Alchemy/shared World Key `localStorage` test keys at the end of the browser smoke test.
- `npx` used its npm-managed cache outside the project; it was not manually deleted.

## Risks

- No persistent automated smoke test was added; this was a one-off bounded worker verification pass.
- Current persistence remains prototype-only browser `localStorage`, not an account-backed contract.
- The browser smoke test used one desktop viewport and accelerated return/sleep timing through the existing debug hook.
- Real-time cave feel over several natural minutes still needs human playtest judgement.
- The known Vite large chunk warning remains.
- The project is not a git repository, so change tracking is limited.

## Memory-worthy notes

- On 2026-05-12, the current Alchemy Lab cave loop passed a controlled desktop browser smoke test at `http://127.0.0.1:5174/`.
- Clean first-run cave state released exactly one pet while keeping the other two waiting on a 30-second stagger.
- Sleep timing matched the intended rule: energy exhaustion sends the pet home first, and sleep starts only after shed return/unload.
- No source fix was warranted in this bounded pass.

## Do not promote to memory

- Temporary Playwright script details.
- Temporary screenshot/log paths.
- Exact one-off random cave route details from the smoke run.
- `npx` cache implementation details.

## Next recommended gate

Run a short real-time cave feel playtest without forced debug timing: watch 2-3 natural pet releases, check harvest density and pet readability, and only then decide whether cave pacing, home-base overlap, or resource reveal needs a small tuning pass.
